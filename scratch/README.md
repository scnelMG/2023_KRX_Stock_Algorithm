# Scratch experiments

`random_rank_scratch.ipynb` creates a random 1–2,000 rank assignment only to inspect the historical submission-file shape. It is not a model, baseline, validation artifact, or portfolio result.

It is deliberately kept outside the repository root so that reviewers encounter the reproducible submission pipeline first.
