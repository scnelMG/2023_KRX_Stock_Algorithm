# 검증 리포트

검증일: 2026-08-12

## 변경 범위

- `window_slicing.ipynb`를 10개 셀의 순차 실행형 제출 파이프라인으로 재구성
- `ParserError`, 순서가 뒤집힌 변수 참조, 탐색 출력 51개, 깨진 차트 출력을 제거
- 랜덤 순위 생성 노트북을 `scratch/`로 이동하고 용도를 명시
- Notion·Drive·코드에서 확인한 사실과 확인되지 않은 성능 근거를 분리한 문서 추가
- 직접 의존성을 고정하고, 외부 데이터·파기 조건에 따른 재현 경계를 보강
- GitHub Support에 보낼 과거 SHA 제거 요청 초안 추가

## 검증 결과

- 모든 `window_slicing.ipynb` 코드 셀의 Python 문법 검사 통과
- 정리된 노트북: 코드 셀 7개, 출력 0개, `ParserError` 문자열·경고 억제 코드 없음
- 합성 2종목 데이터로 feature 결합부터 고유 순위 제출 생성까지 실행: 통과
- `requirements.txt`의 고정 의존성 해석(`pip install --dry-run`): 통과
- `git diff --check`: 통과

## 검증하지 않은 항목

- 대회 원본 데이터로 실행하지 않았습니다. 해당 데이터는 공개 저장소에 없고, Notion의 데이터 이용 조건상 보유 권한 확인 없이 다시 사용하면 안 됩니다.
- 대회 리더보드 점수·RMSE·Sharpe를 재계산하지 않았습니다. 보관된 검증 기록이 없으므로 수치를 새로 주장하지 않습니다.
- GitHub 인프라의 캐시 객체 `3766b565d504faf5a1f3c98198d97058f31ad15c` 삭제는 GitHub Support의 외부 처리 대상입니다.
