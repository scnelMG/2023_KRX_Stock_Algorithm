# 검증 리포트

검증일: 2026-08-11

## 변경 범위

- README를 금융 시계열 포트폴리오 형식으로 재작성
- 데이터 공개 기준 문서 추가
- 재현 가능성 메모 추가
- 표준 `python3` 커널, 의존성 목록, 모델링·제출 파이프라인 메모 추가
- 코드와 문서에만 적용되는 MIT 라이선스 추가
- 대회 데이터·제출 CSV를 Git 전체 이력에서 제거

## 검증 결과

- 신규 대용량 파일 추가 없음
- KRX 대회 데이터와 제출 CSV를 현재 공개 브랜치와 재작성된 `main` 이력에서 제외
- Drive의 KRX 원본/제출 폴더 추가 없음
- `git diff --check` 통과

## 남은 주의 사항

원본 데이터를 내려받아 전체 노트북을 재실행하려면 대회 제공 파일을 로컬 `data/`에 배치하고, `pykrx` 외부 조회가 가능한 환경을 준비해야 합니다.
